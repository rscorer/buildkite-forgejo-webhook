# Buildkite configuration
BUILDKITE_ORG=your-org-name
BUILDKITE_TOKEN=your-api-token-here

# Optional: Webhook port (default: 8080)
WEBHOOK_PORT=8080

# Optional: Enable verbose logging (default: false)
LOG_VERBOSE=false
